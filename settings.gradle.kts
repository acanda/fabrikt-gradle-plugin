pluginManagement {
    repositories {
        mavenCentral()
        gradlePluginPortal()
    }
    plugins {
        kotlin("jvm") version "1.9.10"
    }
}

rootProject.name = "fabrikt-gradle-plugin"
